# iPhone向け 生産数報告PWA 仕様書 v1

## 1. 目的

iPhoneを入力端末として使用し、現場でQRコードを読み取って品番と作業者を特定し、生産数を入力・報告する。

本アプリはネイティブiPhoneアプリではなく、Safari上で動作するPWA（Progressive Web App）として実装する。

---

## 2. 基本方針

- 対象端末：iPhone
- 基準ブラウザ：Safari
- アプリ形式：PWA
- 開発環境：Windows PC + Codex
- QRコード読取：iPhoneのカメラを使用
- QRコードは2種類使用する
  - 品番QR
  - 作業者QR
- 生産数は1以上の整数を入力する
- 報告データにはタイムスタンプ（TS）を含める
- 報告データにはGPS位置情報を含める
- 入力した報告データは端末内へ保存する
- サーバーやAPIへの送信は行わない
- データ一覧画面から未処理データ全件のCSVを生成する
- iPhoneの共有シートを使用してCSVをメールアプリへ渡し、利用者がメールを送信する
- PWAでは実際のメール送信完了を確認しない
- CSVを共有先へ正常に引き渡したデータは「CSV引渡し済み」とする
- 画面は現場での片手操作を考慮し、入力項目と操作数を最小化する
- App Store配布は前提としない
- ホーム画面へ追加して使用できるようにする

---

## 3. 基本業務フロー

1. PWAを起動する
2. 「作業者名」フィールドをタップする
3. 作業者QRを読み取る
4. 作業者名を画面に表示し、以後の報告で保持する
5. 「品番」フィールドをタップする
6. 品番QRを読み取る
7. 品番を画面に表示する
8. 生産数を入力する
9. GPS位置情報を取得する
10. GPS取得失敗時は緯度・経度をNAとする
11. タイムスタンプ（TS）を付与する
12. 内容を確認する
13. 「報告」を押す
14. 報告データを端末内へ「未処理」として保存する
15. 保存完了を画面上で確認できるようにする
16. 後続の生産報告では、PWAを再起動するまで保持している同じ作業者名を使用する

---

## 4. QRコード仕様

QRコードは2種類とする。

### 4.1 品番QR

格納内容：

- 品番

読取後：

- 読み取った品番を画面に表示する

### 4.2 作業者QR

格納内容：

- 作業者名

読取後：

- 読み取った作業者名を画面に表示する

QRの種類はQRコード自体に識別情報を持たせず、画面上の読み取り対象フィールドで判別する。

- 「作業者名」フィールドをタップしてQRを読み取った場合：作業者QRとして扱う
- 「品番」フィールドをタップしてQRを読み取った場合：品番QRとして扱う

---

## 5. 生産数入力

- 生産数は1以上の整数を入力する
- 0、負数、小数は入力不可とする
- 上限は設けない
- 数値以外は入力不可とする
- 数値入力に適したモバイルキーボードを表示する
- 入力欄は現場で操作しやすい大きさとする

---

## 6. GPS

報告データにはiPhoneから取得したGPS位置情報を含める。

記録項目：

- 緯度
- 経度

GPS取得タイミングは報告時を基本とする。

GPS取得に失敗した場合でも報告処理は継続する。

GPSを取得できなかった場合：

- latitude：NA
- longitude：NA

として報告データに記録する。

---

## 7. タイムスタンプ（TS）

各生産報告データにはタイムスタンプを付与する。

タイムスタンプは、生産報告を行った時点を示すデータとして保存する。

オフライン時も端末側でTSを記録し、後からCSVを生成する場合も元のTSを保持する。

---

## 8. 生産報告データ

最低限、以下を1件の生産報告として保持する。

| 項目 | 内容 | 必須 |
|---|---|---|
| report_id | 報告一意ID | 必須 |
| part_number | 品番QRから取得した品番 | 必須 |
| worker_name | 作業者QRから取得した作業者名 | 必須 |
| production_count | 入力した生産数 | 必須 |
| timestamp | 生産報告時のTS | 必須 |
| latitude | GPS緯度。取得失敗時はNA | 必須 |
| longitude | GPS経度。取得失敗時はNA | 必須 |
| csv_status | 未処理 / CSV引渡し済み | 必須 |
| csv_handed_off_at | CSVを共有先へ正常に引き渡した日時 | CSV引渡し後 |

---

## 9. 生産数入力画面

表示項目：

- 作業者名
- 品番
- 生産数
- GPS取得状態
- 未処理件数

操作：

- 作業者名フィールドをタップして作業者QR読取
- 品番フィールドをタップして品番QR読取
- 生産数入力
- 報告
- キャンセル
- QR再読取

---

## 10. オフライン対応

通信できない場合でも入力と端末内保存を継続できるようにする。

- 報告データはIndexedDB等の端末内ストレージへ保存する
- PWAを閉じても報告データを保持する
- サーバーへの同期処理は行わない
- オフライン時に記録したTSとGPSデータをそのまま保持する
- 各報告に一意なreport_idを付与する
- CSV生成はオフラインでも実行可能とする
- メールの実送信可否はiPhoneのメールアプリおよび通信状態に従う

---

## 11. カメラ

用途：

- 作業者QR読取
- 品番QR読取

要件：

- iPhone Safari / PWAからカメラを使用する
- 初回利用時にカメラ使用許可を取得する
- 原則として背面カメラを使用する
- QR読取終了後はカメラを停止する

---

## 12. 作業者情報の保持

- 作業者QRは最初に1回読み取る
- 読み取った作業者名は現在のPWA起動中のみ保持する
- 後続の生産報告には保持している同じ作業者名を書き出す
- 作業者を変更する場合は「作業者名」フィールドをタップし、別の作業者QRを読み取る
- PWAを再起動した場合、作業者名は保持せず、作業者QRを再度読み取る

---


## 13. 画面サイズ・画面方向への対応

iPhoneの画面サイズおよび端末の向きに応じて、画面レイアウトを自動調整するレスポンシブデザインとする。

要件：

- iPhoneの機種ごとの画面幅・高さの違いに自動対応する
- 縦向き（portrait）と横向き（landscape）の両方に対応する
- 端末を回転した場合、画面の再読み込みを必要とせずレイアウトを自動変更する
- 入力欄、ボタン、表示項目は画面からはみ出さないようにする
- 固定ピクセル幅を前提とした画面設計を避ける
- タップ操作に必要なボタンサイズと間隔を維持する
- QR読取画面のカメラ表示領域も縦横の画面方向に合わせて自動調整する
- Safe Area（ノッチ、Dynamic Island、ホームインジケータ領域）を考慮する
- フォントは可能な限り大きくし、現場で視認しやすいサイズを基本とする
- 重要情報（作業者名、品番、生産数、処理状態）は特に大きく表示する
- 小さい補助文字に依存しない
- 文字サイズ変更時にも主要な操作ができるようにする

基本レイアウト方針：

- 縦向き：入力項目を上から下へ配置する
- 横向き：利用可能な横幅に応じて、表示情報と入力操作を横並びにできる構成とする
- 縦横で機能や入力項目は変えず、配置のみ最適化する

---

## 14. 操作性

- 1画面の情報量を少なくする
- フォントは可能な限り大きくする
- ボタンを大きくする
- QR読取を中心にして文字入力を減らす
- 生産数は数値入力のみとする
- 作業者名・品番・生産数を報告前に確認できるようにする
- 正常終了時は明確に完了表示する
- オフラインでも入力作業を止めない
- 未処理状態を確認できるようにする

---

## 15. 初期モックアップ範囲

1. PWA起動
2. 作業者名フィールドのタップ
3. 作業者QR読取
4. 作業者名表示・保持
5. 品番フィールドのタップ
6. 品番QR読取
7. 品番表示
8. 生産数入力
9. GPS取得（失敗時はNA）
10. TS付与
11. 報告
12. 端末内保存
13. 未処理件数表示
14. 後続報告への作業者名引継ぎ
15. PWA再起動時の作業者名リセット
16. データ一覧から未処理データ全件のCSV生成
17. iPhone共有シート起動
18. メールアプリへのCSV添付
19. 利用者によるメール送信
20. CSV引渡し済み状態の記録
21. CSV引渡し済みデータの次回CSV対象外処理
22. メール送信先設定画面
23. 送信先メールアドレスの端末内保存・変更

---

## 16. Codexへの実装指示

この仕様書に記載されていない業務仕様を推測して追加しないこと。

未確定項目については勝手に仕様を決定しないこと。

まず初期モックアップを作成し、iPhone Safari/PWAで以下を確認できる状態にする。

画面はレスポンシブデザインとし、iPhoneの画面サイズ、縦向き、横向きの変更に自動対応すること。

- 作業者名フィールドからの作業者QR読取
- 作業者名の保持と後続報告への引継ぎ
- 品番フィールドからの品番QR読取
- 生産数入力
- GPS取得（失敗時はNA記録）
- TS記録
- 端末内保存
- オフラインでの入力・保存
- iPhoneの画面サイズに応じた自動レイアウト調整
- 縦向き・横向き切替時の自動レイアウト変更
- データ一覧から未処理データ全件のCSV生成
- iPhone共有シート起動
- メールアプリへのCSV添付
- 利用者によるメール送信
- CSV引渡し済み状態の記録
- CSV引渡し済みデータの次回CSV対象外処理
- メール送信先設定画面
- 送信先メールアドレスの保存・変更

必要な開発用フレームワークおよびライブラリは使用してよい。

使用するフレームワーク、ライブラリ、端末内データベース構成については、実装前に候補と理由を提示すること。

仕様書に記載されていない業務仕様や業務上の枠組みを追加しないこと。

---


## 17. データ一覧からのCSVメール送信

モックアップでは、データ一覧画面から未処理データ全件のCSVファイルを生成し、iPhoneの共有機能を使ってメールアプリへ渡す。サーバーやAPIへの送信は行わない。

### 操作フロー

1. データ一覧画面を開く
2. 未処理データ全件を確認する
3. 「メール送信」ボタンをタップする
4. 未処理データ全件からCSVファイルを生成する
5. iPhoneの共有シートを開く
6. 利用者がメールアプリを選択する
7. CSVファイルをメールへ添付する
8. 利用者が宛先・件名等を確認して送信する
9. CSVが共有先へ正常に引き渡された場合、対象データを「CSV引渡し済み」として記録する
10. CSV引渡し済みデータは次回のCSV生成対象から除外する

### CSVに含める項目

- report_id
- part_number
- worker_name
- production_count
- timestamp
- latitude
- longitude
- csv_status
- csv_handed_off_at

GPS取得に失敗した場合は、latitude / longitude に `NA` を出力する。

### 処理状態

- 処理状態は「未処理」または「CSV引渡し済み」とする
- 共有シートを開いただけでは「CSV引渡し済み」にしない
- CSVが共有先へ正常に引き渡された場合に「CSV引渡し済み」とする
- 共有操作をキャンセルした場合は「未処理」のままとする
- 実際のメール送信完了はPWA側から検知できないため、「送信済み」という状態は使用しない

### 実装方針

- CSVはPWA内で生成する
- CSVをメールアプリへ渡す技術的手段としてiPhoneの共有シートを利用する
- 業務上の操作名は「メール送信」とする
- メール送信自体は利用者がiPhone側のメールアプリで行う
- PWAからメールを自動送信する仕様にはしない
- PWAでは実際のメール送信完了を確認しない

---


## 18. メール送信先設定画面

CSVをメール添付で送信するため、送信先メールアドレスを設定・変更できる画面を用意する。

### 表示・入力項目

- 送信先メールアドレス
- 保存ボタン
- キャンセルボタン

### 基本動作

- 利用者が送信先メールアドレスを入力する
- 入力したメールアドレスを端末内に保存する
- 次回以降も同じ送信先を利用できるようにする
- メールアドレスは後から変更できる
- 未設定の場合は、メール送信操作を実行する前に設定を促す

### 入力チェック

- 空欄は不可
- メールアドレス形式を確認する
- 不正な形式の場合は保存しない

### 保存先

- モックアップでは端末内に保存する
- 保存方法はPWAのローカルストレージまたはIndexedDBを使用する
- サーバー側で一元管理する仕様にはしない

### メール送信時

- 設定済みのメールアドレスは、PWA画面上で利用者が送信先を確認するために使用する
- iPhoneの共有シートを起動するWeb Share APIには、メールの宛先欄を指定する機能がない
- 共有シートからメールアプリを選択した場合、宛先欄は空白になることがある
- 宛先欄が空白の場合、利用者が設定画面に表示されたメールアドレスを確認し、メールアプリの宛先欄へ入力または選択する
- PWAからはCSVファイル、件名用のタイトル、および本文「生産実績報告」をメールアプリへ引き渡す
- 宛先の自動入力とCSV添付を同時に行うには、サーバーまたはメール送信APIを使用する別方式が必要となるため、初期モックアップの対象外とする

---

## 19. 現時点の未確定事項

1. 使用する開発用フレームワークおよびライブラリ
2. 端末内データベース構成
3. iPhone実機確認用のHTTPS公開方法
4. 報告後の修正・取消方法
